const USER_URI = "/users";

export const USER_ROUTE = `${USER_URI}/user`;

export const LOGIN_ROUTE = `${USER_URI}/login`;

export const LOGOUT_ROUTE = `${USER_URI}/logout`;

export const PASSWORD_ROUTE = `${USER_URI}/password`;
